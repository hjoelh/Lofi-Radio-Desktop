{
  "name": "lofi-radio-desktop",
  "version": "0.1",
  "scripts": {
    "tauri": "tauri",
    "dev": "tauri dev",
    "build:osx": "tauri build --target universal-apple-darwin --verbose",
    "build:windows": "tauri build --verbose"
  },
  "dependencies": {
    "@tauri-apps/api": "^1.0.2"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^1.0.5"
  }
}
